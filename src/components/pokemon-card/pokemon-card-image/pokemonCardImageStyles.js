import React from "react";
import { StyleSheet } from "react-native";

const pokemonImage = {
  width: "70%",
  aspectRatio: 1,
  marginHorizontal: 6,
};

export const styles = StyleSheet.create({
  pokemonImage,
});
